from utils import config

REWARD = config["blockchain"]["initial_reward"]
INITIAL_BITS = config["blockchain"]["initial_bits"]
DIFFICULTY_ADJUSTMENT_INTERVAL = config["blockchain"]["difficulty_adjustment_interval"]
EXPECTED_MINE_TIME = config["blockchain"]["expected_mine_time"]
